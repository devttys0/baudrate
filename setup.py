#!/usr/bin/env python
from distutils.core import setup

setup(name='baudrate',
      version='0.1',
      description='serial baudrate detector',
      author='Craig Heffner',
      scripts=['src/baudrate'])
